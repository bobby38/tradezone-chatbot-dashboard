# Copy this file to .env.sc and fill the values
# Supabase (Service Role key required for upserts)
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

## Google Search Console auth (choose ONE)

# Option A) Service Account (recommended for weekly local job)
# Paste RAW JSON or BASE64 of your service account credentials.
# Prefer BASE64 for single-line env. Example:
#   GOOGLE_SERVICE_ACCOUNT_KEY=BASE64_ENCODED_JSON
# To base64 your file on macOS:
#   base64 -i "/path/to/credentials.json" | tr -d '\n'
GOOGLE_SERVICE_ACCOUNT_KEY=YOUR_SERVICE_ACCOUNT_KEY_HERE

# Option B) OAuth (if you prefer)
# You may paste RAW JSON or BASE64 of the OAuth client. Example RAW JSON for {web} or {installed} client.
# GOOGLE_OAUTH_CLIENT_KEY={
#   "web": {
#     "client_id": "xxxxxxxxxxxxxxxx.apps.googleusercontent.com",
#     "project_id": "tradezone-sg-project",
#     "auth_uri": "https://accounts.google.com/o/oauth2/auth",
#     "token_uri": "https://oauth2.googleapis.com/token",
#     "auth_provider_x509_cert_url": "https://www.googleapis.com/oauth2/v1/certs",
#     "client_secret": "xxxxxxxxxxxxxxxx",
#     "redirect_uris": ["http://localhost"]
#   }
# }
# GOOGLE_OAUTH_REFRESH_TOKEN=1//your-refresh-token

# Default site
SC_SITE=https://tradezone.sg/
